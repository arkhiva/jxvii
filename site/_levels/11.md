---
layout: level

level: "11"
permalink: "/nP4RwY8mLc7D"
reference: "23064"
title: "Você já ouviu isso antes"

answers:
  - "FANDOM"

hints:
  - triggers:
      - "MORSE"
      - "CÓDIGO"
      - "CODIGO"
    type: warning
    message: "Você já encontrou esse tipo de código antes. Talvez seja hora de ouvi-lo novamente."

reward:
  title: "Você conhece essa palavra"
  message: >
    Fandom.
    Uma pequena referência ao universo do BTS.
    Acho que você sabe exatamente do que estou falando. :^)
  qrcode: "/assets/rewards/23064.jpeg"
  ticket: "JXVII-4Q8F-16KB"

next_level: "/Xq8VmT3aPe6L"
---

Escute

<audio id="level-audio" loop>
    <source src="{{ '/assets/audio/11.mp3' | relative_url }}" type="audio/mpeg">
</audio>

<script>
    const audio = document.getElementById("level-audio");

    async function startAudio() {
        try {
            await audio.play();

            document.removeEventListener("click", startAudio);
            document.removeEventListener("keydown", startAudio);
            document.removeEventListener("touchstart", startAudio);
        } catch {
            // O navegador bloqueou o autoplay.
            // Tentaremos novamente na primeira interação.
        }
    }

    // Tenta imediatamente ao carregar
    startAudio();

    // Se o navegador bloquear, tenta após a primeira interação
    document.addEventListener("click", startAudio);
    document.addEventListener("keydown", startAudio);
    document.addEventListener("touchstart", startAudio);
</script>