# bundle exec jekyll serve

# General settings
site_title: "Desafio JXVII"

# Styles settings
sass:
  sourcemap: never

# Optimization settings
compress_html:
  clippings: all
  comments: ["<!-- ", " -->"]
  endings: all
  blanklines: true
  profile: false
  startings: [html, head, body]
sass:
  style: compressed